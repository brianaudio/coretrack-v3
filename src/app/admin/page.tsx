export default function AdminPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Admin Dashboard</h1>
      <p className="text-gray-600">
        Admin functionality is temporarily disabled during deployment setup.
        Will be restored once the main application is successfully deployed.
      </p>
    </div>
  )
}
