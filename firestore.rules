rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Super permissive rules for initial setup
    // TODO: Tighten these rules after setup is complete
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
